[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "format-commit-msg"
version = "0.1.0"
dependencies = [
  "GitPython",
]
requires-python = ">= 3.9"

[project.entry-points.format_commit_msg]
format = "format_commit_msg:main"
